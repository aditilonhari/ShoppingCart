<h3>Editing Category</h3>

<%= render 'form', category: @category %>

<%= link_to 'Show', @category, {:style=>'color:#FFFFFF;', :class => "btn btn-primary"} %>
<%= link_to 'Back', categories_path , {:style=>'color:#FFFFFF;', :class => "btn btn-primary"}%>
